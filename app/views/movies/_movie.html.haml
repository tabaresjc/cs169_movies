%h1{:id => 'show_movie_title'} Details about #{@movie.title}

%ul#details
  %li
    Rating:
    = @movie.rating
  %li
    Released on:
    = @movie.release_date.strftime("%B %d, %Y")
  %li
    Director:
    = @movie.director

%h3 Description:
%p#description= @movie.description
= link_to 'Edit Movie', edit_movie_path(movie), :id => 'link_edit_movie'


